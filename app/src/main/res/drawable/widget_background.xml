<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Workaround: Omit the icon on API < 23 so it doesn't get stretched horizontally. -->
    <item android:drawable="@color/translucent_background" />
</layer-list>
