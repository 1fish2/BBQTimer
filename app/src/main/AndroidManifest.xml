<?xml version="1.0" encoding="utf-8"?>
<!--
 installLocation=internalOnly so enabling USB mass storage to share files
      with a computer won't delete the app's home screen widgets.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
    <!-- This alarm clock (interval timer) app needs to schedule exact alarms via setAlarmClock(). -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:ignore="ExactAlarmPolicy" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <supports-screens
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/full_backup_content"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:appCategory="productivity"
        android:description="@string/app_description"
        android:icon="@mipmap/ic_launcher"
        android:intentMatchingFlags="enforceIntentFilter"
        android:label="@string/app_name"
        android:name=".BBQTimerApplication"
        android:theme="@style/Theme.App"
        android:enableOnBackInvokedCallback="true"
        android:supportsRtl="false"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="s"
        tools:ignore="GoogleAppIndexingWarning,UnusedAttribute">

        <!-- <profileable android:shell="true"/> -->

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.App.Starting"
            android:configChanges="keyboard|keyboardHidden|screenSize|mcc|mnc"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:exported="true"
            android:intentMatchingFlags="allowNullAction">
            <intent-filter>
                <action android:name="android.intent.action.EDIT" />
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.QUICK_CLOCK" />
                <action android:name="android.intent.action.RUN" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.shortcuts"
                       android:resource="@xml/shortcuts" />
            <!-- https://developer.android.com/guide/topics/ui/multi-window.html -->
            <!--suppress AndroidElementNotAllowed -->
            <layout android:minWidth="@dimen/activityMinWidth"
                    android:minHeight="@dimen/activityMinHeight"/>
        </activity>

        <receiver android:name=".TimerAppWidgetProvider"
                  android:label="@string/app_name"
                  android:exported="false"
                  android:intentMatchingFlags="none"> <!-- Opt out of strict matching so future or
                    manufacturer-specific Intents will get through to super.onReceive(). -->
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_DELETED" />
                <action android:name="android.appwidget.action.APPWIDGET_DISABLED" />
                <action android:name="android.appwidget.action.APPWIDGET_ENABLED" />
                <action android:name="android.appwidget.action.APPWIDGET_ENABLE_AND_UPDATE" />
                <action android:name="android.appwidget.action.APPWIDGET_RESTORED" />
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE_OPTIONS" />
                <action android:name="com.onefishtwo.bbqtimer.ACTION_CYCLE" />
                <action android:name="com.onefishtwo.bbqtimer.ACTION_PAUSE" />
                <action android:name="com.onefishtwo.bbqtimer.ACTION_RESET" />
                <action android:name="com.onefishtwo.bbqtimer.ACTION_RUN" />
                <action android:name="com.onefishtwo.bbqtimer.ACTION_RUN_PAUSE" />
                <action android:name="com.onefishtwo.bbqtimer.ACTION_STOP" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/app_widget_info" />
        </receiver>

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.onefishtwo.bbqtimer.ACTION_ALARM" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ResumeReceiver"
            android:enabled="true"
            android:exported="false" >
            <intent-filter>
                <action android:name="android.intent.action.LOCALE_CHANGED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.RUN" />
                <!-- TODO: QUICKBOOT_POWERON? ACTION_LOCKED_BOOT_COMPLETED? -->
            </intent-filter>
        </receiver>
    </application>

</manifest>
